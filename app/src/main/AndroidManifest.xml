<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.yu.bigproject">
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".base.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".sign.activity.SignActivity" />
        <activity android:name=".ShowActivity" />
        <activity android:name=".home.activity.DetailActivity" />
        <activity android:name=".my.activity.PersonNewActivity" />
        <activity android:name=".my.activity.UserCircleActivity" />
        <activity android:name=".my.activity.UserFootActivity" />
        <activity android:name=".my.activity.UserWalletActivity" />
        <activity android:name=".my.activity.UserAddressActivity" />
        <activity android:name=".my.activity.AddAddressActivity"></activity>
    </application>

</manifest>